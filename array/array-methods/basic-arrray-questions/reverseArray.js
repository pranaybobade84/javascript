// Reverse an Array
// Write a function that takes an array as input and returns it reversed.

function reverseArray(arr) {
  //   return arr.sort((a, b) => b -a);
  return arr.reverse();
}

const input = [1, 2, 3, 4, 5];

console.log(reverseArray(input));
